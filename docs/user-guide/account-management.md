# Управление аккаунтами

DeBot позволяет управлять всеми вашими аккаунтами прямо из Telegram.

### `.listaccs`

Показывает список всех аккаунтов, хранящихся в базе данных, их статус (включен/отключен) и время последней активности (для отключенных).

-   🟢 - Аккаунт включен и активен (или будет запущен после перезагрузки).
-   🔴 - Аккаунт выключен и не будет запущен.

### `.addacc <имя_аккаунта>`

Запускает интерактивный диалог в текущем чате для добавления нового аккаунта.

-   **`<имя_аккаунта>`**: Уникальное имя для нового аккаунта (например, `work_account`).

Процесс добавления включает в себя:
1.  Запрос `API ID` и `API Hash`.
2.  Проверку учетных данных и автоматический запрос пароля, если включена двухфакторная аутентификация (2FA).
3.  Проверку на дубликаты: бот не позволит добавить аккаунт, если Telegram ID уже существует в базе.
4.  Запрос дополнительных параметров: язык, статус активации, выбор устройства.

### `.delacc <имя_аккаунта>`

Удаляет аккаунт и все связанные с ним данные (сессию, настройки модулей) из базы данных.

-   **`<имя_аккаунта>`**: Имя аккаунта, который нужно удалить.

Команда запросит подтверждение перед выполнением необратимого удаления.

### `.toggleacc <имя_аккаунта>`

Включает или выключает указанный аккаунт.

-   **`<имя_аккаунта>`**: Имя аккаунта для переключения статуса.

Отключенные аккаунты не будут запускаться при старте бота. Это полезно для временной деактивации без удаления сессии.

> **Важно:** После выполнения команд `.addacc`, `.delacc`, `.toggleacc` требуется перезапуск бота (`docker-compose restart userbot`), чтобы изменения вступили в силу.
